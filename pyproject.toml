[project]
name = "smartcut"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "av==12.3.0",
    "numpy<2.0.0",
    "tqdm>=4.67.1",
]

[project.scripts]
smartcut = "smartcut.__main__:main"
